<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Letter of Appreciation | DevPlayground</title>
    <style>
      /* --- Base Reset --- */
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: "Poppins", sans-serif;
        transition: background 0.3s, color 0.3s;
      }

      body {
        background-color: #f5f5f5;
        color: #333;
        display: flex;
        flex-direction: column;
        min-height: 100vh;
      }

      header {
        background-color: #0078d7;
        color: white;
        padding: 1rem 2rem;
        text-align: center;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.15);
      }

      header h1 {
        font-size: 1.8rem;
        letter-spacing: 1px;
      }

      /* --- Appreciation Section --- */
      #appreciation {
        max-width: 800px;
        margin: 50px auto;
        padding: 30px;
        background-color: white;
        border-radius: 12px;
        box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
        animation: fadeIn 1.2s ease-in-out;
      }

      #appreciation h2 {
        text-align: center;
        color: #0078d7;
        margin-bottom: 20px;
      }

      #appreciation p {
        font-size: 1.1rem;
        line-height: 1.8;
        color: #444;
        margin-bottom: 15px;
      }

      #appreciation p:last-child {
        text-align: right;
        font-weight: bold;
        color: #222;
      }

      /* --- Footer --- */
      footer {
        margin-top: auto;
        text-align: center;
        padding: 1rem;
        background-color: #0078d7;
        color: white;
        font-size: 0.9rem;
      }

      /* --- Button Styling --- */
      .toggle-btn {
        position: fixed;
        top: 15px;
        right: 20px;
        background-color: #0078d7;
        color: white;
        border: none;
        padding: 8px 15px;
        border-radius: 6px;
        cursor: pointer;
        font-weight: bold;
      }

      .toggle-btn:hover {
        background-color: #005fa3;
      }

      /* --- Dark Mode --- */
      body.dark {
        background-color: #121212;
        color: #e0e0e0;
      }

      body.dark header,
      body.dark footer {
        background-color: #1e1e1e;
      }

      body.dark #appreciation {
        background-color: #1e1e1e;
        color: #ddd;
        box-shadow: 0 0 15px rgba(255, 255, 255, 0.05);
      }

      body.dark #appreciation h2 {
        color: #4dabf7;
      }

      /* --- Animation --- */
      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
    </style>
  </head>
  <body>
    <button class="toggle-btn" id="themeToggle">🌙 Dark Mode</button>

    <header>
      <h1>Kingsley’s Appreciation Page</h1>
    </header>

    <section id="appreciation">
      <h2>Letter of Appreciation</h2>
      <p>Dear <strong>Mrs Kate</strong>,</p>
      <p>
        I wanted to take a moment to genuinely appreciate your unwavering support throughout my career journey. Your encouragement, guidance, and belief in my potential have been instrumental to my progress and growth.
Thank you once again for being such a dependable pillar in my professional journey. People like you make the difference between ambition and achievement.
      </p>
      <p>Thank you for being a part of my story.</p>
      <p>Kingsley</p>
    </section>

    <footer>© 2025 Kingsley Donatus. All rights reserved.</footer>

    <script>
      const toggleBtn = document.getElementById("themeToggle");
      const body = document.body;

      toggleBtn.addEventListener("click", () => {
        body.classList.toggle("dark");
        toggleBtn.textContent = body.classList.contains("dark")
          ? "☀️ Light Mode"
          : "🌙 Dark Mode";
      });
    </script>
  </body>
</html>
